# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Datos contables — NO subir a este repo.
# El repositorio es público y la contabilidad identifica a personas reales
# (montos cobrados por colaborador). Estos archivos se generan localmente con
# `pnpm sync:finanzas`. Ver docs/finanzas.md antes de cambiar esta regla.
datos-crudos/
src/data/movimientos.json

# Credenciales del sincronizador
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
